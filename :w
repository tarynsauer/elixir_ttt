defmodule UtilsTest do
  use ExUnit.Case
  import Utils 

  test "cell has marker" do
    assert has_marker("X") == 3 
  end

  test "number of rows in the board" do
    board = [1, 2, 3, 4, 5, 6, 7, 8, 9]
    assert row_count(board) == 3 
  end

end
